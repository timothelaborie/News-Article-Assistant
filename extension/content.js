// console.log("Content script has been injected!");

// chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
// 	if (request.message === "get_html") {
// 	  let response = document.documentElement.outerHTML;

// 	if (location.href.includes("bbc.com")){
// 		response = ""
// 		document.querySelectorAll("div[data-component='text-block']").forEach(element => {
// 			response += element.innerText
// 		});
// 	}


// 	  sendResponse(response);  // Sending back the HTML to the popup
// 	}
//   });
  